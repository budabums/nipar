// Assuming _title$frame is not defined or is null
let title = {};
_title$frame !== null && _title$frame !== undefined ? _title$frame : title.frame = 'group';
console.log(title.frame); // Output: 'group'
